version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Coalesce all action bumps into a single PR rather than one per action.
    groups:
      github-actions:
        patterns:
          - '*'
