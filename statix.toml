# These files are generated by `nixos-generate-config` and carry a
# "Do not modify this file!" header, so they're excluded from linting
# rather than restructured (a regeneration would just revert any changes).
ignore = [
  "hosts/nixos/huijatoo/hardware.nix",
  "modules/hardware/huijatoo.nix",
]
